# Current State

Last verified: 2026-09-02 (session 2 close — hardening tranche 2 COMPLETE)

- **Branch:** master @ `3fe2b25` — 64 commits ahead of `5cef867` (session-1 baseline); `origin/master` NOT pushed (never push; no tags). Working tree clean except the untracked handoff files (`HANDOFF.md`, `CURRENT_STATE.md`). `git worktree list` = 1, `git stash list` empty.
- **Changed this session (by area):** `packages/core` (`network_error`/`empty_body` errorCodes); `packages/contracts` (`CONTRACT_VERSION` 0.5.0, `ProposalDto.claimedAt`); `packages/testing` (4 new goldens + drivers, goldens re-recorded once, conformance: `claimedAt`, `poisonCount` ×3, first-terminal-wins, fence-before-short-circuit); `packages/adapters-sqlite` (schema v8, FIFO `whenFree` slot, `SqliteWriteGate`/`writeGateFor`, first-terminal-wins `endAttempt`, close-on-refuse, split into `src/sqlite/*`); `packages/adapters-memory` (parity); `packages/host` (`claimedAt` stamping + reconcile window, `resetPass`, `failQuietly`, split into `turn/{history-assembly,submit,harness-driver,pass-types}.ts`, barrel unchanged in public names); `packages/runner-local` (store-owned `poisonCount`, fence order on every settle branch + logged `endAttempt` failure, slice-D E2E); `packages/transport-http` (`sse.ts` `controller.error`, `ProposalDto.claimedAt` projection); `packages/client` (finite-`seq` dedupe, README); `packages/react` (`settlePhase`, chat-switch guards, drain isolation, `finishReason`, README); `packages/agentkit` (version `0.5.0`); docs (`CHANGELOG.md` 0.5.0, `docs/adr/0014-*.md`, contracts/roadmap/ports/architecture, both playbooks, sqlite README); README/DEVELOPING `#v0.5.0` pins; `bun.lock` version line.
- **Build/test (final gate at `325c181`):** `bun install --frozen-lockfile` OK; `bun run typecheck` clean (13 projects); `bun test` → 1648 pass / 1 skip / 0 fail (113 files); `bun run lint` clean (371 files); `bun run build` clean; `bun run build:umbrella` → 377 files; `bun run smoke:umbrella` + `node scripts/node-smoke.mjs` pass.
- **Key decisions (session 2):** release prep without tag (umbrella `0.5.0`, pins `#v0.5.0`; user tags per DEVELOPING.md); sqlite schema v8 folded in (fresh dev db); `poisonCount` store-owned on `running → abandoned`, idempotent, first terminal status wins on `endAttempt` with the fence checked first; FIFO gate = real slot on `txGate`; config store queues via per-handle `writeGateFor(db)`; SSE pump errors REJECT the body (clean EOF = task terminal only); hooks settle phase from task status when no terminal event; pass-state types and the moved turn helpers stay OFF the host barrel; defaults confirmed as shipped; Phase 6 = pure moves only (turn-runner 1368 lines — the ≤700 target was not reachable without restructuring, deliberately not done).
- **Blockers:** none. Tranche complete. Next is the user's call (tag `v0.5.0` / OpenPCB migration / P6); roadmap Later holds the deferred items surfaced this session.
